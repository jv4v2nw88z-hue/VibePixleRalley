{
  "name": "rally-pixel",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "description": "Pixel-art rally time-trial game — gravel, tarmac and snow stages, pacenotes, surface-specific physics and a garage upgrade tree.",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "npm run build && wrangler dev",
    "deploy": "npm run build && wrangler deploy"
  },
  "devDependencies": {
    "@cloudflare/vite-plugin": "^1.47.0",
    "vite": "^8.1.5",
    "wrangler": "^4.114.0"
  }
}
